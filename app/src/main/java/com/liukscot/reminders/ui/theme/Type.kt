package com.liukscot.reminders.ui.theme

import androidx.compose.material3.Typography
import androidx.compose.ui.text.font.FontFamily

// Default Material 3 type scale — no customization until a concrete reason emerges.
val Typography = Typography()

// Design system's `--font-mono` (JetBrains Mono, ui-monospace fallback chain)
// used for every count/numeric label (list counts, smart-card counts, times).
val MonoFontFamily = FontFamily.Monospace
