package com.liukscot.reminders.ui.theme

import androidx.compose.ui.graphics.Color

// Material Flow Design System tokens — shared across the app family, do not
// hand-pick new values here. See docs/design-system.md.
// https://github.com/LiukScot/Material-Flow-Design-System

// Ink surfaces (shared, dark-only)
val InkBase = Color(0xFF010102)
val InkBg = Color(0xFF030304)
val InkCard = Color(0xFF1C1C22)
val InkRaised = Color(0xFF232329)
val InkHover = Color(0xFF2A2A31)
val InkPressed = Color(0xFF323239)

// Text tones (shared, dark-only)
val TextStrong = Color(0xFFF6F5FB)
val TextBody = Color(0xFFD9D7E3)
val TextMuted = Color(0xFFB0AEBF)
val TextFaint = Color(0xFF7E7C8E)

// Borders / hairlines (shared) — `--line-1` / `--line-2`
val Line1 = Color(0x0FFFFFFF)

// Semantic status (shared)
val StatusSuccess = Color(0xFF7FD6A6)
val StatusWarning = Color(0xFFF2C879)
val StatusDanger = Color(0xFFFF8B8B)

// Flow accent — Reminders' theme is "ember" (amber → coral)
val EmberFlowA = Color(0xFFFFC27F)
val EmberFlowB = Color(0xFFFF8F7F)
val EmberAccentSolid = Color(0xFFFFA98F)
val EmberAccentSoft = Color(0xFFFFBCA6)
val EmberTextOnAccent = Color(0xFF2C150E)
